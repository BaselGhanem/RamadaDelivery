Ramada Delivery | نظام الكشوفات المتقدم (Premium System)
تطبيق ويب متكامل يعمل من جانب العميل (Client-Side) مصمم لتسهيل العملية المالية لشركات التوصيل والخدمات اللوجستية. يقوم النظام بقراءة بيانات ملفات Excel، وتجميع الطلبات حسب العملاء، وحساب المستحقات المالية بناءً على حالة التوصيل، ومن ثم توليد كشوفات حساب احترافية بصيغة PDF.

🌟 المميزات الرئيسية
محرك تحليل بيانات قوي: يدعم السحب والإفلات لملفات .xlsx، .xls، و .csv.

ربط أعمدة ديناميكي: يمكن للمستخدم تحديد الأعمدة يدويًا (اسم العميل، الحالة، الهاتف، المبالغ) مما يجعله متوافقًا مع أي تنسيق Excel.

فلترة وحساب ذكي: يقوم النظام بجمع المبالغ فقط للشحنات التي حالتها "تم التسليم"، ويستثني باقي الحالات (مثل المرتجعات أو المؤجلات) من المجموع النهائي.

واجهة تفاعلية:

عرض شبكي (Grid View) لجميع العملاء مع إمكانية البحث السريع.

إحصائيات فورية (عدد الشحنات، صافي المبلغ).

توليد تقارير PDF:

تصميم أفقي (Landscape A4) أنيق واحترافي.

يدعم تعدد الصفحات تلقائياً مع ترويسة وتذييل لكل صفحة.

يتضمن ملخصاً مالياً (Summary) في الصفحة الأولى.

خيارات التصدير والمشاركة:

تحميل فردي: تنزيل كشف لعميل محدد.

تحميل الكل (Bulk): ضغط جميع كشوفات العملاء في ملف .zip واحد بضغطة زر.

واتساب: زر مباشر لفتح محادثة مع العميل لإرسال الكشف.

تصميم عصري: مبني باستخدام Bootstrap 5 (RTL) مع ثيم لوني مخصص (برتقالي/رمادي).

🛠️ التقنيات المستخدمة
الأساس: HTML5, CSS3, Vanilla JavaScript.

التصميم: Bootstrap 5 (RTL), FontAwesome 6, Animate.css, Google Fonts (Tajawal).

المكتبات البرمجية (CDN):

XLSX (SheetJS): لقراءة ومعالجة ملفات الإكسل.

html2pdf.js: لتحويل التقارير إلى PDF.

JSZip: لإنشاء الملفات المضغوطة.

FileSaver.js: لحفظ الملفات على جهاز المستخدم.

🚀 طريقة التشغيل
بما أن التطبيق يعمل بالكامل على المتصفح ويعتمد على مكتبات CDN، لا حاجة لتثبيت أي برمجيات خادم (مثل Node.js أو PHP).

حمل ملف index.html.

تأكد من وجود اتصال بالإنترنت (لتحميل المكتبات والخطوط في المرة الأولى).

افتح الملف مباشرة في أي متصفح حديث (Chrome, Edge, Firefox).

📖 دليل الاستخدام
رفع الملف: قم بسحب وإفلات ملف الإكسل (Manifest) في المربع المخصص.

إعدادات القراءة:

حدد رقم سطر العناوين (Header Row).

ربط الأعمدة: اختر العمود الذي يمثل "اسم العميل" والعمود الذي يمثل "الحالة".

أعمدة الجمع: اختر الأعمدة التي تريد جمع قيمها (مثل: مبلغ التحصيل، أجور التوصيل).

المعالجة: اضغط على زر "تحليل وعرض البيانات".

التصدير:

يمكنك استعراض أي عميل بالضغط على اسمه.

لتحميل ملف واحد اضغط "تحميل PDF".

لتحميل كافة العملاء دفعة واحدة، اضغط على زر "تحميل الكل (ZIP)" في القائمة الجانبية.

⚙️ قيود ومعايير النظام
معيار الحالة: يعتمد النظام برمجياً على البحث عن جملة "تم التسليم" في عمود الحالة لإدراج المبلغ في المجموع. أي حالة أخرى ستظهر في الكشف بخلفية رمادية ولن يتم جمع مبلغها.

🎨 التخصيص (الألوان)
لتغيير ألوان العلامة التجارية، يمكنك تعديل متغيرات CSS في بداية ملف الكود:

CSS

:root {
    --primary-brand: #f3822b;   /* اللون الرئيسي - البرتقالي */
    --secondary-brand: #4e4e4e; /* اللون الثانوي - الرمادي */
    --bg-body: #f8fafc;
}
📄 الترخيص
هذا البرنامج تم تطويره خصيصاً لـ Ramada Delivery.

تطوير: باسل غانم
